query missionById($missionId: String) {
  mission(missionId: $missionId) {
    id
    missionName
    speakers {
      name
      label
    }
    log {
      timestamp
      speakerName
      text
      id
    }
  }
}