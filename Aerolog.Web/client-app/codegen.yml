schema: 'https://localhost:55160/graphql'
documents: './src/**/*.graphql'
generates:
  ./src/types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-apollo-client-helpers
      - typed-document-node
